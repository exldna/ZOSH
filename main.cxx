# include <iostream>
# include <vector>

using Graph = std::vector<std::vector<int>>;

int main()
{
	size_t m, n;
	std::cin >> m >> n;

	Graph g = Graph(n, std::vector<int> (m));

	for (int i = 0; i < n; ++i) {
		for 
	}

	return 0;
}
